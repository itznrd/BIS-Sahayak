/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 15 + React 19 — no extra flags needed.
  // Use: npm run dev -- --turbopack   for faster hot-reload.
  devIndicators: false,
};

module.exports = nextConfig;

